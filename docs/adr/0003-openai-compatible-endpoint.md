# 采用 OpenAI 兼容协议，端点可配置

LLM 调用只对接 OpenAI 兼容的 chat/completions 协议，base URL、密钥、模型名均由用户配置，可指向云端服务商，也可指向本地兼容端点（如 Ollama）。不绑定任何单一厂商 SDK。

理由：A1 模式下用户自带密钥，绑定单一厂商会把配置、接口、CORS 处理写死，且无法接纳用户已有的本地模型。走兼容协议后，新增服务商只需改 base URL 与模型名，无代码锁入。代价是所有服务商必须在该协议子集内行为一致。
