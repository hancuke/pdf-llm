# 纯客户端架构，无自有后端

本应用是纯浏览器网站：PDF 在浏览器内用 pdf.js 解析，文档字节与用户标注不上传、不存储到任何自有服务器；LLM 请求由浏览器直连服务商，使用用户自带并保存在 localStorage 的 API 密钥。

选择这一形态是因为核心价值主张是"本地 PDF + 隐私"——用户不愿把文档传出去。代价是：无后端意味着无法做服务端缓存、无法隐藏 API 密钥（直面 CORS 与本地设备泄露风险）、无法做跨设备同步。若未来需要多端同步或共享注释，需重新引入后端，届时该决定被推翻。
