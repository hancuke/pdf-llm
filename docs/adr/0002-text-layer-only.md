# 仅支持含文本层的 PDF，排除扫描件与 OCR

本应用只处理内嵌文本层的 PDF；对无文本层的扫描件/图片 PDF，打开即提示"不支持"，不做 OCR。

理由：纯前端 OCR（tesseract.js）体积大、速度慢、对中文与公式精度有限，且会显著拖慢首版交付。明确排除该场景，是为了把首版范围锁死在"有文本层"这一可可靠提取的子集上。若未来要覆盖扫描件，需作为新决策重新引入 OCR 能力。
