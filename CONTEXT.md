# PDF 辅助理解（PDF-LLM）

一个纯浏览器端的网站：用户在本地打开 PDF，选中其中文字，由 LLM 结合上下文辅助理解。PDF 文件与用户的标注始终只存在于浏览器中、不上传至任何服务器、没有任何自有后端；但为提供「朗读」「音标」，选中内容的片段会发往对应第三方功能接口（有意、可控偏离，详见 ADR-0013），且可在设置中关闭。

## Language

**文档（Document）**：
用户通过浏览器文件选择/拖拽打开的本地 PDF 文件。其字节与解析结果只存在于浏览器内存/IndexedDB，绝不上传到任何服务器。
_Avoid_: 文件、资料、book

**干净题目（Clean Title）**：
从 PDF 元数据或首页标题提取的可读标题（如《Attention Is All You Need》），区别于原始文件名；作为一行 meta 上下文传给 LLM，使其调动已有知识。
_Avoid_: 文件名、标题、文档名

**选中内容（Selection）**：
用户在 PDF 渲染层高亮选中的一段文字。是后续送给 LLM 的核心输入。
_Avoid_: 高亮、划词、片段

**API 密钥（API Key）**：
用户自己的 LLM 服务商密钥，由用户在界面中填写，保存在浏览器 localStorage。应用用它代表用户直连 LLM，不经过任何自有后端。
_Avoid_: token、凭证、密码

**上下文（Context）**：
LLM 除选中内容外额外看到的辅助文字，范围是选中内容所在的段落全文（按文本提取层切分出的段落边界确定）；本应用额外附带该文档的「干净题目」。模型可借其对本文的潜在已有认知与通用知识来解释，但焦点始终是选中内容（见「选中聚焦澄清」）。
_Avoid_: 前后文、背景

**上下文降级（Context Fallback）**：
当 PDF 能抽出文字但无法判定段落边界（如多栏论文乱序）时，上下文退化为选中内容前后各 N 句（按换行/标点切分）。
_Avoid_: 兜底、退回

**操作菜单（Action Sheet / 浮动泡泡栏）**：
选中文字后弹出的浮动选项列表，列出若干快捷操作。视觉形态为「浮动泡泡栏」——圆角胶囊、指向选中文字的小箭头，采用「沉静编辑式」视觉语言（见 `ActionSheet.vue`，视觉风格见 ADR-0012）。其**内容**为 LLM 快捷操作 + 复制 + 取消，不在此层新增全选/查找/分享等系统级动作（ADR-0009）。
_Avoid_: 右键菜单、弹窗、工具栏

**底部 Tab 栏（Bottom Tab Bar）**：
移动端（窄屏）主入口的下沉布局：屏幕底部的标签栏，承载打开/目录/搜索/对话/生词本/设置等主操作；此时顶部工具栏收敛为标题 + 页码胶囊 + 主题切换，其余入口下沉到此栏（ADR-0014；视觉风格见 ADR-0012）。
_Avoid_: 底部导航、底栏、tabbar

**文本层（Text Layer）**：
PDF 内嵌的可提取文字。本应用只支持含文本层的 PDF；无文本层的扫描件/图片 PDF 不在范围内，打开时提示不支持。
_Avoid_: 文字、OCR 层

**快捷操作（Quick Action）**：
操作菜单中的单项指令，如"翻译选中内容""解释选中内容"，背后是预置提示词。
_Avoid_: 命令、按钮

**自定义动作（Custom Action）**：
用户自建的快捷操作，由用户编写提示词模板，保存在 localStorage，跨刷新保留。
_Avoid_: 我的动作、个人命令

**选中聚焦澄清（Selection-Focused Clarification）**：
本应用对 LLM 的核心定位——以用户选中内容为唯一理解焦点，借助模型对该文档的潜在全文认知与通用知识加以解释（类比、举例、关联），但不得离题扩写。
_Avoid_: 整篇问答、自由发散

**讲解风格（Explanation Style）**：
全局设置，控制 LLM 回答的通俗程度，取值 默认 / 通俗大白话 / 小学生级，对所有快捷动作生效。
_Avoid_: 难度、受众、语气

**对话（Conversation）**：
围绕一次"选中内容 + 上下文"开启的多轮追问会话。本应用中为「每文档单一活跃线程」：新选中会重设上下文锚点但并入同一对话、保留历史，用户可手动清空；刷新页面即清空（ADR-0004）。
_Avoid_: 聊天、会话

**对话历史（Conversation History）**：
某次对话中已经发生的问答记录。本应用中不持久化，刷新页面即清空。
_Avoid_: 记录、缓存

**兼容端点（Endpoint）**：
用户配置的 OpenAI 兼容 API 地址（base URL）+ 密钥 + 模型名，可指向云端或本地模型（如 Ollama）。
_Avoid_: 服务商、API、接口

**目录（Outline）**：
PDF 文件自带的、嵌入在元数据中的层级目录树（即 PDF 规范里称作 "Bookmark" 的那棵树）。只读，来源于文件本身，应用不修改它。
_Avoid_: 书签、大纲、Bookmark、TOC

**书签（Bookmark）**：
用户主动创建的、保存在浏览器中的"位置记忆"，仅记录页码与滚动位置，不含任何文档文字或对话。与 PDF 自带的"目录"无关。因其不持久化摘录，故不违反 ADR-0004。
_Avoid_: 收藏、标记、Outline、目录

**面板（Pane）**：
界面中承载一类内容的独立区域。本应用为可折叠多栏布局：目录面板、PDF 面板、对话面板、生词本面板，均可由工具栏独立展开/收起（ADR-0016 新增生词本面板）。
_Avoid_: 栏、面板区、区域

**工具栏（Toolbar）**：
顶部常驻三区式图标工具栏：左区承载品牌与面板开关，中区承载阅读位置（页码字段）与缩放（含适合宽度/适合页面等预设），右区承载搜索、对话、主题切换等工具。桌面端图标为主 + tooltip；移动端（≤768px）收敛为标题 + 页码胶囊 + 主题，其余入口下沉到底部 Tab 栏（ADR-0014；视觉风格见 ADR-0012）。
_Avoid_: 顶栏、导航栏、header

**设置面板（Settings Panel）**：
用户调整应用偏好与连接配置的表面，由工具栏或底部 Tab 栏的「设置」入口打开。重构后（见 ADR-0017）采用 macOS 系统设置式信息架构：桌面端为右侧滑出面板（PDF 留在背后 scrim 中），左栏为 4 个分类（通用 / 模型服务 / 朗读 / 动作）、右为详情；移动端为 iOS 式下钻（分类列表 → 详情）。所有改动即时写入本机 localStorage（即时应用模型），无「保存/取消」；保留「测试连接」作为端点校验。视觉遵循「沉静编辑式」（ADR-0012），不使用毛玻璃。
_Avoid_: 设置页、偏好、options、设置项

**朗读面板（Read-Aloud Panel）**：
用户触发「朗读」后出现的、选区作用域的临时表面，不属于三栏布局里的常驻「面板（Pane）」。上半部为传输条，分两行：第一行 重播 / 播放暂停 / 进度条 / 时间；第二行 朗读语速、朗读音色、⋯更多（含音调、下载、睡眠定时）。下半部为把选区文字按词切分后的「朗读文字视图」（见「朗读文字视图」）。触发即出现；朗读结束后面板不自动关闭（保留传输条与下载，直至用户手动关闭）；可收起为「迷你播放器」（见「迷你播放器」）；不持久化为布局状态。定位为选区作用域的「随听」，不自动连读全文档（见 ADR-0015）。
_Avoid_: 朗读栏、TTS 面板、语音面板

**朗读文字视图（Read-Aloud Text View）**：
朗读面板下半部渲染选区原文的区域，文字被切分为可交互的「词单元」（见「词单元」），供点击查看音标与单词语音。与 PDF 原生文本层无关——是独立于 PDF 渲染层的副本。刻意**不与音频播放进度同步**（非逐词跟读/karaoke 视图，见 ADR-0015）；其差异化价值在「点词查词」的词汇面，而非高亮跟读。
_Avoid_: 文本区、字幕、跟读视图

**迷你播放器（Mini Player）**：
朗读面板收起后保留的常驻细条，是选区作用域朗读的表面之一（非三栏布局的「面板（Pane）」）。继续播放当前选区语音，提供展开回朗读面板 / 暂停 / 停止；让用户边浏览 PDF 或借 LLM 理解边听。受纯浏览器标签限制，不是后台/锁屏播放。
_Avoid_: 悬浮球、后台播放条

**朗读语速（Read-Aloud Rate）**：
TTS 速率，面板内第二行常驻的快捷控制（−/＋ 或档位），写入 `ttsRate`；不再仅藏于设置。
_Avoid_: 速度、倍速

**朗读音色（Read-Aloud Voice）**：
TTS 嗓音（如晓晓 / 云希 / Emma），面板内第二行可直接切换，写入 `ttsVoice`；不再仅藏于设置。
_Avoid_: 声音、嗓音、发音人

**睡眠定时（Sleep Timer）**：
朗读「⋯更多」抽屉中的可选定时器，到点自动停止播放；纯前端计时，不持久化。
_Avoid_: 定时关闭、休眠

**词汇卡（Vocab Card）**：
朗读文字视图中点词弹出的临时卡片（非独立表面），承载该词的音标（IPA）、单词语音与「收藏」入口；是朗读文字视图的词汇面。收藏即写入「生词本」（见「生词本」、ADR-0016）。
_Avoid_: 单词卡、释义卡、词典卡

**生词本（Vocabulary Book）**：
用户从词汇卡收藏的词语集合，持久化于浏览器 localStorage 的浏览表面（ADR-0016）。桌面为可折叠「生词本面板」，移动端下沉为底部 Tab 栏入口，与 目录/对话 并列。每条词条含：词、音标（IPA，可能为空）、语境句（收藏时的选区上下文）、来源文档干净题目、页码、收藏时间。刻意保存用户选中文本（与「书签」仅存位置不同），但文本仅留浏览器、不上传（ADR-0001）。
_Avoid_: 单词本、生词表、收藏夹

**词单元（Word Unit）**：
朗读文字视图中可被单独点击/高亮的最小语言单位。切分用浏览器内置 `Intl.Segmenter`（`granularity: 'word'`），中英文（含混排）统一处理，无需后端或额外依赖（决策见 grill 会话）。每个词单元可承载音标与独立发声。
_Avoid_: 单词、token、词

**单词音标（Word Phonetics）**：
点击词单元时在该词下方弹出的音标（英文为 IPA，本期不做中文音标）。数据来自外部免费词典 API，而非本地词典——此为对 `ADR-0001` 纯客户端原则的有意偏离，理由见 `ADR-0011`。按词缓存以避免重复请求。
_Avoid_: 拼音、注音

**单词发音（Word Pronunciation）**：
点击词单元时自动播放的该词读音。由现有 TTS 代理**重合成单个词**得到（复用 `tts.ts`），音色与整段朗读一致，任意语言可用；不依赖外部词典 API（见 `ADR-0011`）。
_Avoid_: 单词朗读、单词语音
